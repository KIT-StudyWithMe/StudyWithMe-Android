package de.pse.kit.studywithme.model.data

/**
 * Enum class for the pickable options of the sessionfrequency in the chip component
 *
 * @constructor Create empty Session frequency
 */
enum class SessionFrequency {
    ONCE,
    WEEKLY,
    TWOWEEKLY,
    THREEWEEKLY,
    MONTHLY
}