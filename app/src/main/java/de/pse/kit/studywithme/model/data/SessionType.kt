package de.pse.kit.studywithme.model.data

/**
 * Enum class for the pickable options of the sessiontype in the chip component
 *
 * @constructor Create empty Session type
 */
enum class SessionType {
    ONLINE,
    PRESENCE,
    HYBRID
}